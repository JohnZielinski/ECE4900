#include <Servo.h>

Servo servo;
int laser_din=2; //set input data pin
int count;
void Dispense(int numpills)
{for(count=0; count<=numpills;)
{
  if(digitalRead(laser_din)==HIGH) //laser on and checking 
  {
    servo.write(180);
    delay(5);
    
//    servo.write(180);
//    delay(1);
  }
  count=count=1;
 servo.write(180);
//  
}
}

void setup() 
{
servo.attach(9);
pinMode(laser_din,INPUT);
Serial.begin(9600);
Serial.println("Enter number of pills");
Dispense(2);
}

void loop()
{
 Dispense(2);

}

